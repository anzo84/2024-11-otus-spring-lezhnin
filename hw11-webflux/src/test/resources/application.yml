spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:
    pool:
      enabled: true
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: schema.sql
